/*

//1316.py

// 파이썬을 이용하면 더 쉽게 짤 수 있을 것 같은데 ...
// C/ C++ 에서도 Set 자료형을 사용해 보는 것이 좋을 것 같아서 한번 시도해봤다.
// 있는 바퀴를 만들지 말라고 하였거늘 ... C++ 복습할 겸 stack 과 set 자료형을 만들어 보았다.

// 기본적인 알고리즘은 이렇다
// 입력된 문자열을 스택에 모두 push한다.
// 맨 위의 문자를 pop 한다. 다음 원소가 pop된 문자와 같다면 전부 pop한다.
// 현재 문자가 set에 없다면 추가한다. set에 이미 있다면 이 문자열은 그룹 단어가 아니다
// 이 과정을 끝까지 거쳐서 '그룹 단어가 아니다' 라고 판별되지 않았다면 이 단어는 그룹단어이다.

#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>


class myStack{

private:
	void* top;
	int StackSize; //스택에 저장할 자료형을 몇개 담을 것인지 

	stack = malloc(sizeof() * StackSize);

public:

	myStack(int StackSize, );
	
}



*/